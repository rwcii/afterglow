[pytest]
markers =
    rig_roles: declare the on-air rig board roles a test needs (dut, stimulus, observer, peer_a, peer_b); the rig fixture opens only these and green-skips when a port is unconfigured.
